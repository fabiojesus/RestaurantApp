@using Recodme.Academy.RestaurantApp.WebApplication.Models.MenuViewModels;
@model DishViewModel

<div class="card">
    <div class="card-header bg-greenish-green">
        <h2 class="float-left">
            @ViewData["Title"]
        </h2>
    </div>
    <div class="card-body">
        <div class="ml-5 mr-5">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group col-lg-3 col-md-6 col-sm-12">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control ml-2" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="form-group col-lg-3 col-md-6 col-sm-12">
                    <label asp-for="DietaryRestrictionId" class="control-label"></label>
                    <div class="ml-2">
                        <select asp-items="@ViewBag.DietaryRestrictions" asp-for="DietaryRestrictionId" class="form-control fk-select">
                        </select>
                    </div>

                    <span asp-validation-for="DietaryRestrictionId" class="text-danger"></span>
                </div>
                <div class="form-group ml-4">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts{
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
<script>
    $(document).ready(function () {
        $('.fk-select').select2({
            placeholder: "Select a restriction",
            allowClear: true
        });
    });
</script>